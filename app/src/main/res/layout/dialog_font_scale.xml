<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingTop="16dp"
    android:paddingEnd="20dp"
    android:paddingBottom="16dp">

    <!-- 当前缩放比例大字展示 -->
    <TextView
        android:id="@+id/tvFontScaleValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:textColor="@color/brand"
        android:textSize="32sp"
        android:textStyle="bold"
        tools:text="100%" />

    <TextView
        android:id="@+id/tvFontScaleHint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="2dp"
        android:text="@string/font_scale_range"
        android:textColor="@color/text_muted"
        android:textSize="12sp" />

    <!-- 横轴拖动滑块 -->
    <SeekBar
        android:id="@+id/seekFontScale"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:max="90"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:progress="0"
        android:progressBackgroundTint="@color/border"
        android:progressTint="@color/brand"
        android:thumbTint="@color/brand" />

    <!-- 刻度端点说明 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="8dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvFontScaleMin"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="100%"
            android:textColor="@color/text_dim"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/tvFontScaleMax"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="190%"
            android:textColor="@color/text_dim"
            android:textSize="12sp" />
    </LinearLayout>

</LinearLayout>
